# --- Core (pipeline + ARIMA + visuals) ---
pandas==2.2.2
numpy==1.26.4
openpyxl==3.1.5         # read .xlsx
xlrd==2.0.1             # read legacy .xls
matplotlib==3.8.4
seaborn==0.13.2
statsmodels==0.14.2     # ARIMA
scikit-learn==1.5.1
xgboost==2.0.3
mlxtend==0.23.1         # permutation feature importance

# --- Optional: notebooks / parquet / plotly image export ---
# jupyter==1.0.0
# ipykernel==6.29.5
# pyarrow==16.1.0        # if you save parquet
# plotly==5.23.0
# kaleido==0.2.1         # plotly static image export

# --- Optional: TensorFlow (for LSTM). Enable ONE of the blocks below ---

# Apple Silicon (M1/M2/M3) on macOS:
# tensorflow-macos==2.15.1 ; platform_system == "Darwin" and platform_machine == "arm64"
# tensorflow-metal==1.1.0  ; platform_system == "Darwin" and platform_machine == "arm64"

# Others (Linux/Windows, or Intel mac):
# tensorflow==2.15.1       ; not (platform_system == "Darwin" and platform_machine == "arm64")

# --- Optional: custom modules you referenced ---
# PFFRA  # install from your source repo if you use it

